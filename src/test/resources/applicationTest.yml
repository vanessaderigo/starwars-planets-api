spring:

  application:
    name: starwars-planets-api

  datasource:
    url: jdbc:mysql://localhost:3306/starwars
    username: user
    password: 1234567

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true